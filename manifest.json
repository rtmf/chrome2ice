{
	"manifest_version": 2,
	"name": "chrome2ice",
	"version": "0.0.2",

	"description": "a Chrome plugin to determine the currently playing tab(s) and provide such data in an easily accessible manner for external apps",

	"background": {
		"persistent": true,
		"scripts": ["settings.js","chrome2ice.js"]
	},
	"permissions": [
		"tabs", "http://*/"
	]
}
